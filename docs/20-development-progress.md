# 20 — Development Progress

Living tracker. Update this file in the same change as the work it describes — never mark something complete that wasn't actually implemented and at least minimally verified (see `18-testing-qa.md`'s "what done means").

## Phase 1 — Foundation

| # | Milestone | Status | Notes |
|---|---|---|---|
| 1a | Repo/tooling scaffold | **Done** | Next.js 15.5.23 (App Router, TS strict) + Tailwind v4 + shadcn/ui (`base-nova` style) + ESLint 9 (flat config via `FlatCompat`) + Prettier defaults. `npm run build` and `npm run lint` both pass clean. Full feature-based folder structure created per `02-system-architecture.md`. `.env.example` documents every required variable. Git initialized, `Jemwanzs/JMS-SalesApp` remote connected, `main`/`development` branches created, initial commit pushed. |
| 1b | Core tenancy + RBAC migration | **Applied and spot-verified** | `supabase/migrations/0001_core_tenancy_and_rbac.sql` was run against the live project (`bajjwqlrxzamvdwjzxdq`, applied by the user via Supabase Studio's SQL Editor) and verified end-to-end from the app side: all 11 tables reachable, all 30 seeded permission rows present, `is_tenant_member()` callable without error. `tests/pgtap/001_tenant_isolation.sql` (20 assertions) is written, covering RLS on `tenants`/`tenant_memberships`/`locations`/`roles`/`role_permissions`/`user_role_assignments`, the no-membership-sees-nothing case, the permission catalog's global readability, and suspension folding into `has_permission()` — **not yet executed**, since it needs a local Supabase stack (`supabase start`, which needs Docker Desktop running — Docker is installed but the daemon wasn't running when this was written) rather than running fixture-creating tests against the shared live project. `supabase init` has been run, so `supabase/config.toml` exists for local dev. Running `supabase test db` once Docker is up is the next concrete action. |
| 1c | Supabase Auth wiring | **Done and verified end-to-end** | Sign-up, login, logout, password-reset request/confirm, and the `/api/auth/callback` PKCE code-exchange route are all implemented (`features/auth/`) and backed by a real `AuthService`. Session-refresh middleware (`middleware.ts` + `lib/supabase/middleware.ts`) is wired at the root. Verified against the live project: `AuthService.signUp()` correctly reached Supabase's own email-sending path (confirmed by hitting its default-provider rate limit — see Decisions below); a full `TenantService.createTenant()` bootstrap run (via `admin.createUser` to skip the email quota) produced a tenant with the correct slug, an active owner membership, all 3 default roles seeded with the correct permission counts (Sales User: 3, Supervisor: 6, Tenant Administrator: all 30), and the owner correctly assigned to Tenant Administrator — then cleaned up. `npm run build`/`lint`/`tsc --noEmit` all pass. **Not yet verified**: an actual browser click-through of the React forms themselves (no browser automation tool available in this environment) — the service-layer logic they call has been proven directly instead. |
| 1d | Tenant onboarding wizard | **Done and verified end-to-end** (Steps 3-6 are honest placeholders) | Full 7-step wizard shell (`features/onboarding/components/onboarding-wizard.tsx`) with a progress indicator. Steps 1 (Business Details: type/website/anniversary/currency/timezone) and 2 (primary Location + 7-day hours grid, closed-all-day toggle per day) are real forms backed by real `TenantService` methods (`updateBusinessDetails`, `upsertPrimaryLocation`, `setLocationHours`) and existing schema — no new tables needed. Steps 3-6 (Products, Historical Import, Invite Users, Subscription) are deliberately honest "coming in a later phase" cards with a Skip button, not faked functionality, since Products/Import/Invite/Billing don't exist until Phases 2a/5/4b/6 respectively. Step 7 finishes into `/sales`. Entry routing: `resolveActiveTenant()` (`lib/tenant/resolve-active-tenant.ts`) now also reports `needsOnboarding` (a tenant with zero `locations` rows) — sign-up, sign-in, and the root page all route first-timers into `/onboarding` instead of `/sales`, and back to `/sales` once a location exists. **Verified against the live project**: seeded a fresh tenant (zero locations), confirmed root-page redirect landed on `/onboarding`; ran the actual `TenantService` methods under the owner's real RLS-respecting session (not service-role) and confirmed `tenants.business_type/website/anniversary_date/currency/timezone` and all 7 `location_hours` rows (including Sunday correctly closed with null times) persisted exactly as submitted; re-checked the root-page redirect afterward and confirmed it now lands on `/sales`. |
| 1e | `can()` + permission hooks | **Done and verified end-to-end** | `hooks/tenant-context.tsx` adds `TenantProvider`/`useTenant()`/`usePermission()`, wired into the tenant layout (`app/(tenant)/t/[tenantSlug]/layout.tsx`), which resolves the signed-in user's permission set once via `getMyPermissions()` and hydrates it down to client components. Verified against the live project: seeded a real Tenant Administrator user, logged in through the actual running app (not just the service layer), and confirmed `/t/[slug]` rendered "Business-wide sales visibility" (gated on `usePermission("sales.view_all")`) — proving the full path from RLS-backed SQL function through the server layout through client context actually works, not just that it compiles. |
| 1f | Mobile-first app shell + PWA | **Done and verified end-to-end** (next-intl deferred) | Brand mark (`app/icon.svg`, `app/favicon.ico`, `app/apple-icon.png`, `components/shared/logo.tsx`), the ~430px mobile shell constraint, `app/manifest.ts` (name/icons/theme_color `#10786A`/background_color `#FAF3E6`, served at `/manifest.webmanifest`), a minimal installable-but-not-yet-caching service worker (`public/sw.js` + `components/shared/service-worker-registration.tsx`), and the persistent permission-gated bottom nav (`components/shared/bottom-nav.tsx`: Sales/Analytics/Reports/More, wired into a new `(dashboard)/layout.tsx`). Tenant routes restructured: `sales`/`analytics`/`reports`/`more` placeholder pages under `(dashboard)`, `/t/[slug]` now redirects straight into `/sales` (the golden path, spec S13). **Verified against the live project**, including the permission gating specifically: seeded a tenant with one Tenant Administrator and one plain Sales User, logged in as each through the real running app, and confirmed the Administrator's nav shows all 4 tabs while the Sales User's correctly omits Reports (no `reports.view` grant on that role) — not just "the code looks right," an actual observed difference in rendered output between two real roles. next-intl (i18n scaffold) intentionally deferred — no UI copy needs translation yet and every other 1f piece was needed to unblock Phase 2. |
| 1g | Platform admin foundation | **Done and verified end-to-end** | `supabase/migrations/0004_platform_admin_foundation.sql` (`platform_admins` table, RLS enabled with **zero** policies — authenticated/anon get no access at all) and `supabase/seeds/001_platform_admin_bootstrap.sql` (idempotent super_admin grant for `PLATFORM_ADMIN_BOOTSTRAP_EMAIL`, never hardcoded in application code) both applied to the live project. `PlatformAdminService.isPlatformAdmin`/`getDashboardKpis` are real, service-role-only implementations. `app/(platform-admin)/admin/{layout,page}.tsx` — separate dark shell, guards via `isPlatformAdmin()` and redirects non-admins to `/`, real KPI cards (tenant/user counts). **Verified against the live project** via a temporary scratch route (deleted after use, see Decisions below): granted `platform_admins` to a seeded test profile and confirmed `isPlatformAdmin()` returns `true` for it and `false` for an ungranted profile; confirmed an anon-key client querying `platform_admins` directly gets 0 rows back (RLS denies the whole surface, not just filters it) even though the row exists. `startImpersonation` remains a Phase 7 stub. |

## Phase 2 — Sales Engine (2a–2i done; REVERSE deferred)

| # | Milestone | Status | Notes |
|---|---|---|---|
| 2a | Products (+ edit, image upload, activate/deactivate) | **Done and verified end-to-end** | `products`/`product_images` tables (migration 0005) plus a `product-images` Storage bucket + RLS (migrations 0007/0008). `ProductService` now has real `create/update/listActive/listAll/setStatus/archive/setImage/removeImage`; `reorder()` (drag-and-drop) remains stubbed — a later increment. Images upload direct-to-Storage from the browser (`features/products/components/product-image-upload.tsx`) at creation time or via replace/remove in the edit dialog (`edit-product-dialog.tsx`); `products.image_url` stays a denormalized cache of the current `product_images` primary row's public URL. Activate/deactivate toggles `active ⇄ inactive` (gated on `products.edit`, same as edit); archive (gated on the more specific `products.archive`) is unchanged and still one-way in the UI. Deactivated/archived products are automatically excluded from `listActive()` (the sales-capture grid) while historical sales are unaffected (they keep their own snapshot regardless — see 2d). Product thumbnails were also shrunk (management list: 48px; sales-capture grid: fixed 96px-tall image area instead of `aspect-square`) for a more compact layout. `app/(tenant)/t/[tenantSlug]/(dashboard)/products/page.tsx` now renders `ProductManagementList` (thumbnail, status badge, Edit/Activate-Deactivate/Archive per row) instead of a bare name+Archive list. |
| 2b | Business day (manual open/close) | **Done and verified end-to-end** (auto open/close + reopen deferred) | `business_days` table + `business_day_status` enum (migration 0005). `BusinessDayService.getEffectiveTimezone/getTodayBusinessDay/openDay/closeDay` are real; `closeDay()` computes `aggregates` (`grossSales`, `transactionCount`) directly from that day's non-voided sales. `reopenDay()` is now real -- see the 2h row below for the scoped design (reason + approval, no MFA yet). pg_cron auto open/close is Phase 2f, not implemented — manual open/close only for now. |
| 2c | Sale numbering | **Done and verified end-to-end** | `sale_number_sequences` table + `assign_sale_number()` `BEFORE INSERT` trigger (migration 0005), atomic via `INSERT ... ON CONFLICT ... DO UPDATE ... RETURNING` — no explicit row locking needed. Format is hardcoded `SALE-{YYYY}-{000001}` for now; reading a tenant-configurable template from `tenant_settings` is a fast-follow, not blocking a working default. Replacement sales created by a correction (2e) get their own fresh number through the same trigger. |
| 2d | Sales capture (idempotent) | **Done and verified end-to-end** | `sales` table (migration 0005) — deliberately has **no UPDATE/DELETE RLS policy at all**, and still doesn't after 2e/2g (see below — mutation happens exclusively through two SQL functions, not a policy). `SalesService.recordSale()` snapshots product name/image/expected-price at sale time, catches the `23505` unique-violation on `(tenant_id, idempotency_key)` and falls back to returning the existing row as a successful idempotent replay. Real UI: `app/(tenant)/t/[tenantSlug]/(dashboard)/sales/page.tsx` (business day gate, Open Business Day button, product grid), `features/sales/components/{product-grid,record-sale-sheet,open-business-day-button}.tsx`, `sonner` toast confirmation on success. |
| 2e | Sale edit-window / historical correction / void | **Done and verified end-to-end** (REVERSE not implemented) | `sale_corrections` table (migration 0006, immutable ledger — no insert/update/delete policy, written only by the functions below) plus two `SECURITY DEFINER` Postgres functions, `void_sale`/`correct_sale`, which are now the **only** way a sale's status can ever change post-insert (`sales` still has zero direct UPDATE/DELETE policy — the migration-0005 invariant holds through this increment, not around it). `correct_sale` branches on whether the actor is the original recorder and still inside the tenant's edit window (`tenant_settings['sale_edit_window_minutes']`, default 15) — inside it, `sales.edit_window` self-service-edits (void original + insert a replacement sale, linked via `sale_corrections.replacement_sale_id`); outside it, `sales.correct_historical` is required instead. VOID and CORRECT are implemented; REVERSE is a documented future increment, not silently dropped. `SalesService.voidSale/correctSale` are thin RPC wrappers; `voidSale()` is no longer a stub. |
| 2g | Approval engine v1 | **Done and verified end-to-end** | Generic `approval_requests` table (migration 0006) dispatched by `type` — a request is **always** created, even when a tenant doesn't require review, written pre-resolved as `auto_approved` so the audit trail and dispatch path never branch between "logged" and "not logged." `void_sale`/`correct_sale` check `tenant_settings['sale_void_requires_approval']`/`['sale_correction_requires_approval']` (both default `false` — self-approve unless a tenant opts in) and either apply immediately or leave the request `pending`. `resolve_approval_request()` (also `SECURITY DEFINER`) is the only way a pending request gets decided — approve dispatches to the same internal apply-helpers `void_sale`/`correct_sale` use, so the "immediate" and "approved-later" code paths can never drift apart; reject just closes the request with no side effect. New `approvals.manage` permission. `ApprovalService.listPending/resolve` + `app/(tenant)/t/[tenantSlug]/(dashboard)/approvals/page.tsx` (reviewer inbox, redirects non-holders away) + `features/sales/components/{approvals-inbox-list,approval-row-actions}.tsx`. |
| 2f | pg_cron business-day automation | **Done and verified end-to-end** | `supabase/migrations/0011_business_day_sweep_and_report_jobs.sql`, applied live by the user (pg_cron already enabled on the project): `run_business_day_sweep()` (`SECURITY DEFINER`, never callable by `authenticated`) sweeps every active location, resolving today's effective hours (`special_hours` overriding `location_hours`, same fallback pattern as `BusinessDayService.getEffectiveTimezone`) and transitioning `business_days` rows — creates a `scheduled` row ahead of open time, `scheduled -> open` at the scheduled open time, `open -> closed` at the scheduled close time (aggregates computed identically to `BusinessDayService.closeDay`), and closes a never-opened `scheduled` day directly (zero aggregates) if its window elapsed before the sweep ever ran — plus relocking any expired `reopened` day by delegating to `auto_relock_expired_business_day()` (migration 0009) so the aggregate-recompute logic isn't duplicated. Scheduled via `cron.schedule('business-day-sweep', '*/5 * * * *', ...)`, idempotently re-scheduled (unschedule-by-name first). New `report_jobs` table is the outbox (RLS enabled, **zero** policies — service-role only, same posture as `platform_admins`): the sweep inserts a `daily_business_day_report` row on every auto-close, inside the same transaction as the close. `app/api/cron/outbox/route.ts` (Vercel Cron, `CRON_SECRET` bearer-auth via `vercel.json`'s `*/5 * * * *` schedule) drains it using the service-role client — claims up to 20 pending jobs, calls `ReportService.generateDailyReport()`, and since that's still a Phase 3d stub, every job currently fails with an honest "not yet implemented" and stays `pending` (retryable) until 5 attempts, then flips to `failed` — the drain mechanism itself is real and complete, it just has nothing successful to produce yet. **Deliberately deferred**: `notifications`/`notification_preferences` tables (doc 03 §3.6) — fanning out a business-day-closed notification needs a recipient-targeting decision (which permission/role gets notified) not yet made, same "explicit decision before guessing" posture as 2h's MFA deferral. Manual open/close/reopen (`BusinessDayService`) are untouched and still work independently of the sweep. `npm run build`/`lint` both pass clean. **Verified against the live project** via the established scratch-route pattern (12/12 assertions): a location with an open window straddling "now" was correctly `scheduled -> open`ed; a `special_hours` override correctly suppressed automation despite `location_hours` saying open; a directly-seeded `scheduled` day whose window had already elapsed without ever opening was closed directly with zero aggregates; a location with no hours configured at all was correctly left untouched; a directly-seeded `reopened` day with an expired window was relocked with recomputed aggregates via the sweep's delegation to `auto_relock_expired_business_day()`; a second sweep pass correctly transitioned `open -> closed` with the right gross/count and queued exactly one `report_jobs` row with the right payload; `authenticated` was confirmed denied direct execution of the sweep function; the outbox route correctly rejected a wrong `CRON_SECRET` and, called repeatedly, correctly retried an honestly-failing job (Phase 3d stub) up to 5 attempts before flipping it to `failed`. **Bonus real-world confirmation, not staged**: the live sweep had already auto-closed a genuinely manually-opened business day for the user's own test tenant ("JMS Solutions Kinya") at its scheduled close time during this verification window, correctly recomputing aggregates (1 real sale) and queuing a real `report_jobs` row — that row's retry count was reset to `pending`/0 attempts afterward since the rapid-fire test calls would otherwise have burned its real retry budget in seconds instead of the ~25 minutes real Vercel Cron ticks would take. All scratch tenants/users/data cleaned up afterward; the scratch route itself was deleted. |
| 2f-bug | Pre-existing sale-number collision (found during 2f verification, not caused by it) | **Fixed and verified end-to-end** | `sale_number_sequences` (migration 0005) scopes its counter per `(tenant_id, location_id, year)` — deliberately per-location, per `08-sales-engine.md`'s numbering spec (a tenant-configurable template can include a "branch prefix" precisely so locations can run independent receipt-book sequences). But `sales.sale_number` was only unique per `(tenant_id, sale_number)`, tenant-wide, so two locations under the same tenant both on the still-hardcoded default template `SALE-{YYYY}-{000001}` (no location-distinguishing token yet) collided on their first sale of the year. Fixed in `supabase/migrations/0012_fix_sale_number_uniqueness_scope.sql`: drops the old constraint, adds `unique (tenant_id, location_id, sale_number)` — widened to match the sequence's actual scope, rather than narrowing the sequence to be tenant-wide, since the per-location design is intentional. Audited every other unique constraint across all migrations for the same class of scope mismatch (sequence scope vs. constraint scope) — no others found. **Applied and verified against the live project** via the established scratch-route pattern (2/2 assertions): two locations under the same tenant both correctly got `SALE-2026-000001` on their first sale without colliding; a second sale at the first location correctly continued its own sequence at `SALE-2026-000002`, confirming the widened constraint didn't disturb per-location sequencing itself. All scratch data cleaned up afterward; the scratch route was deleted. |
| 2i | Sales history | **Done and verified end-to-end** (PDF export deliberately out of scope, see below) | `SalesService.listRecent()` + `app/(tenant)/t/[tenantSlug]/(dashboard)/sales-history/page.tsx` + `features/sales/components/{sale-history-list,sale-history-filters}.tsx` (status badges, Void/Correct dialogs permission-gated per row). Relies entirely on the existing `sales_select` RLS policy (migration 0005) for the view_own/view_all split — no new filtering logic needed. `listRecent()` gained `dateFrom`/`dateTo` (inclusive `sale_date` bounds, using the composite `(tenant_id, sale_date)` index already in migration 0005) and `search` (matches `sale_number` or the snapshotted `product_name_snapshot` via `ilike`, `%`/`_` escaped before building the `.or()` filter — deliberately matches the sale's own snapshot rather than a live join to `products`, consistent with 2d's "sales keep their own snapshot regardless" decision). `SaleHistoryFilters` (client) pushes `from`/`to`/`q` as URL search params so the filtered list is a real server-rendered query, not a client-side filter over an already-truncated page; the page raises `listRecent`'s cap from 100 to 500 rows whenever any filter is active, since a narrowed query is exactly when someone wants more than the last 100 (e.g. exporting a month). "Export CSV" in `sale-history-list.tsx` builds a CSV client-side from the currently-loaded (already-filtered) rows via a Blob + `<a download>` — no server round-trip, no new endpoint. **PDF export deliberately deferred, not silently dropped**: nothing in this app currently generates PDFs (`ReportService.generateDailyReport` — Phase 3d — is still a stub, and Phase 3's scheduled-report/outbox machinery is the natural home for real document generation, not an ad-hoc button here); CSV alone satisfies the actual need (a full data export) without a new dependency for a placeholder. Wired into the More menu (previously a disabled "Coming soon" row); "Approvals" is also appended to the More menu, but only for users who actually hold `approvals.manage` — unlike every other row it isn't shown-but-disabled for everyone, since most users will never have anything to review. **Verified against the live project** via the established scratch-route pattern (9/9 assertions): unfiltered query returned all 3 seeded sales; `dateFrom` correctly excluded an earlier-dated sale (inclusive bound confirmed on the boundary date); `dateFrom`+`dateTo` together correctly isolated exactly one sale; search by exact `sale_number` and by partial, case-insensitive product name both matched correctly; a no-match search returned an empty array rather than erroring; search input containing literal `%`/`_` characters didn't throw or behave unexpectedly (escaping confirmed working); combining `search` with `dateFrom` correctly ANDed both conditions. All scratch data cleaned up afterward; the scratch route itself was deleted. |
| 2h | Business day reopen | **Done and verified end-to-end** (scoped: no MFA yet, per explicit decision) | `reopen_business_day()` + `auto_relock_expired_business_day()` `SECURITY DEFINER` functions (migration 0009); `resolve_approval_request()` gained a third dispatch branch (`business_day_reopen`) via `create or replace` — a full redefinition, not an edit to the already-applied migration 0006. No new tables/columns: the reason lives in the `approval_requests` row, same pattern as void/correct. Routes through the 2g engine (`tenant_settings['business_day_reopen_requires_approval']`, default `false`). The bounded "reopen until HH:MM" window (max 12h out) auto-relocks **lazily** — `BusinessDayService.getTodayBusinessDay()` checks on every read whether a `reopened` day's window has expired and calls the relock function before returning, since 2f's pg_cron sweep doesn't exist yet; this is load-bearing correctness, not a stopgap. `SalesService.recordSale()` now accepts `status IN ('open', 'reopened')`. UI: `features/sales/components/reopen-business-day-dialog.tsx` (reason + time picker), wired into `app/(tenant)/t/[tenantSlug]/(dashboard)/sales/page.tsx` (closed-day state now offers Reopen instead of just a locked message; a `reopened` day shows a "Reopened until HH:MM" banner and behaves like open for capture). MFA-gating deferred to Phase 4 per explicit decision — this flow's shape doesn't need to change when MFA lands, it just gets an extra gate in front of the same `reopen_business_day()` call. |

**Reopen (2h) and the product photo-view toggle verified together against the live project** via the same temporary scratch-route pattern: a permission-less staffer was denied `reopen_business_day`; a reopen-until more than 12h out was rejected; an owner's reopen with no approval configured applied immediately; a sale was recorded successfully while the day was `reopened`; after waiting past the window, `getTodayBusinessDay()` correctly auto-relocked the day to `closed` **with aggregates recomputed to include the sale recorded during the reopened window** (this caught a real bug — see Decisions below); with `business_day_reopen_requires_approval=true`, a further reopen correctly went `pending_approval`, was resolved via `ApprovalService.resolve()`, and the day was actually reopened; a day that was already `reopened` correctly could not be reopened again. Separately, `products.show_name_in_photo_view` was confirmed to default `true`, settable `false` at creation, and toggleable back via `update()`, with the raw column value matching the service's report. 17/17 assertions passed; all test data cleaned up afterward.

**Product images verified against the live project** via the same temporary scratch-route pattern: uploaded a real (tiny in-memory PNG) file as the owner and confirmed `products.edit` gates every write (a "Sales User" staffer was denied both the Storage upload and a direct `ProductService.update()` call); created a product with a pre-uploaded image and confirmed the `product_images` row + `products.image_url` were set correctly; updated name/description/price; deactivated (disappeared from `listActive`, stayed in `listAll`) and reactivated; replaced the image and confirmed the **old Storage object was actually deleted**, not just superseded; removed the image entirely and confirmed both the DB row and the Storage object were gone; archived and confirmed it dropped out of `listAll`. 17/17 assertions passed, but only after a real bug was caught and fixed mid-verification — see the Storage SELECT-policy gotcha in Decisions below (migration 0008).

**Verified against the live project** via the same temporary scratch-route pattern as 1g/earlier-2 (see Decisions below):
- **2a–2d** (unchanged from the prior entry): created a product, opened a business day, recorded a sale (got a `SALE-YYYY-000001`-shaped number back), replayed the identical request with the same idempotency key (got the same row back, `replayed: true`, no second sale-number consumed), recorded a second distinct sale (got `...000002`), confirmed exactly 2 sale rows exist despite 3 `recordSale()` calls, then closed the day and confirmed `aggregates.grossSales`/`transactionCount` matched the 2 real sales exactly (excluding the replay). 15/15 assertions passed.
- **2e/2g**: a permission-less "Sales User" staffer was correctly denied by `void_sale`; an owner's void with no approval setting applied immediately and left a self-approved `sale_corrections`/`auto_approved` `approval_requests` row; an owner's in-window correction applied immediately, marked the original `corrected`, and created a replacement sale with the new amount, `status='open'`, and its own valid `SALE-YYYY-000006`-shaped number; after setting `sale_void_requires_approval=true`, a further void correctly went `pending_approval` and left the sale untouched; the staffer (neither requester nor `approvals.manage`) was correctly blocked from seeing that pending request by RLS, while the owner's `ApprovalService.listPending()` saw it; `resolve(approved)` actually voided the sale, `resolve(rejected)` left a different sale untouched, and a second `resolve()` on an already-decided request was correctly rejected; sales-history RLS was reconfirmed from a different angle (owner/view_all saw ≥5 sales, the staffer/view_own who recorded nothing saw 0). 18/18 assertions passed. All test tenants/users/data cleaned up afterward in both runs.

**Phase 2 is now fully done** except REVERSE (deferred from 2e, third documented mutation type, not yet needed) and **2h**'s MFA-gating (deferred to Phase 4 per explicit decision — the approval-routing half exists via 2g). A pre-existing sale-numbering bug was found during 2f's verification and fixed (2f-bug row above), pending live apply.

## Phase 3 — Analytics & Reports (3a–3e done; weekly/monthly/custom report types, Resend delivery, Smart alerts/Daily Pulse deliberately deferred)

| # | Milestone | Status | Notes |
|---|---|---|---|
| 3a | Daily aggregate computation on close | **Done and verified end-to-end** | Already existed before Phase 3 formally started — `business_days.aggregates` (`grossSales`/`transactionCount`) computed on every close, manual or automatic (2b/2f). Nothing new needed here; listed for roadmap completeness. |
| 3b | KPI dashboard (permission-gated date filters) + product analytics | **Done and verified end-to-end** (user/location breakdowns, scheduled reports, insights engine not yet — later Phase 3 increments) | `AnalyticsService.getKpis`/`getProductPerformance` (previously stubs). KPIs (spec §33): Total Sales, Transactions, Average Sale, Highest/Lowest Sale, Products Sold, Active Sales Users. `analytics.view_own`/`view_all` (a gate independent of and stricter than `sales.view_own`/`view_all` — a Supervisor can hold `sales.view_all` for Sales History while only holding `analytics.view_own` for this dashboard, by design) restricts the query to the caller's own sales unless `view_all` is held; `analytics.past_dates`/`date_range` are enforced **inside `AnalyticsService` itself**, not just hidden in the UI — a non-today range throws without `past_dates`, a genuine multi-day range throws without `date_range` even with `past_dates`, per `11-analytics-reports.md`'s note that this can't be cleanly expressed as an RLS policy. `activeSalesUsersCount` is `null` (not `0`) when the caller lacks `analytics.all_users`, so the UI can distinguish "not permitted" from "genuinely zero." Product performance groups by `product_id` using the **current** catalog name/image (a live join to `products`), not each sale's own snapshot — intentional per `08-sales-engine.md`'s snapshot-vs-current decision (verified directly: a product renamed after its sale showed the new name in the performance list). Aggregation happens in application code after a bounded raw-row fetch, same pattern `BusinessDayService.closeDay` already uses, rather than a new SQL aggregation function for this first increment. New `lib/utils/date-ranges.ts` resolves the spec's preset buttons (Today/Yesterday/This Week/Last Week/This Month/Last Month, week starting Sunday to match `location_hours`' existing convention) against a tenant's effective timezone, never raw UTC. UI: `app/(tenant)/t/[tenantSlug]/(dashboard)/analytics/page.tsx` (previously a placeholder) + `features/analytics/components/{analytics-filters,kpi-cards,product-performance-list}.tsx` — preset buttons and the custom-date/range inputs the caller can't use are hidden outright (not shown-disabled), matching the More menu's Approvals-row convention. Stat tiles use the app's existing text tokens (no chart-color palette needed — a stat tile explicitly doesn't require one); the product-performance ranked list uses one sequential hue (`bg-primary`, the same accent already used for buttons app-wide) since it's a single series, not a categorical multi-series chart — no new palette introduced or needing validation this increment. **Verified against the live project** via the established scratch-route pattern (8/8 assertions): `view_all` KPIs correctly totaled across two users with a voided sale correctly excluded from every figure; `view_own` KPIs correctly restricted to one user's sales with `activeSalesUsersCount` correctly `null`; `analytics.past_dates` correctly rejected a non-today range without it; `analytics.date_range` correctly rejected a multi-day range without it even with `past_dates` held; a single past date was correctly allowed with only `past_dates`; product performance was correctly grouped/sorted and used the product's current (post-rename) name; `analytics.products` was correctly enforced; `resolvePreset('this_week')` returned a valid, consistent range. All scratch data cleaned up afterward; the scratch route was deleted. |
| 3c | Scheduled report generation — daily only | **Done and verified end-to-end** (weekly/monthly/custom report types, Resend email delivery, and the notifications fan-out are later increments, not this one) | New `reports` table (migration 0013, RLS gated on `reports.view`, no write policy — only the service-role outbox-drain worker ever writes it). `ReportService.generateDailyReport()` (previously a stub) is now real: reads `business_days.aggregates` for gross sales/transaction count/average sale, computes top product and top sales person by summing `actual_amount` per `product_name_snapshot`/`recorded_by` over that day's non-voided sales, and a vs-previous-day percentage (previous **closed** day for the same tenant+location). Deliberately uses each sale's own snapshot fields, not a live join to the current catalog/profile — a daily report is a historical record of that specific day, so it should read the same regardless of later renames, the opposite reasoning from `AnalyticsService`'s arbitrary-range product performance (see 3b above) which intentionally uses current identity — the two services differ on purpose, not by oversight. `app/api/cron/outbox/route.ts` (already calling `generateDailyReport` since 2f, previously always failing on the stub) now links a successfully-drained job to the `reports` row it produced via `report_id`. New `app/(tenant)/t/[tenantSlug]/(dashboard)/reports/page.tsx` (previously a placeholder) + `features/reports/components/report-list.tsx` render the generated reports (gross sales, transactions, average, top product, top sales person, vs-previous-day). **Verified against the live project** via the established scratch-route pattern (9/9 assertions): a report generated directly from `generateDailyReport()` had the correct type/period/status and every payload figure (including correctly excluding a voided sale from top-product/top-seller consideration, and a correct +80% vs-previous-day calculation); RLS correctly returned zero rows to a tenant member with no `reports.view` grant (filtered, not an error); a full outbox-loop test (seed a real pending `report_jobs` row, hit the drain route, confirm it completed and linked to a real `reports` row for the right business day) passed; `listReports()` returned both generated reports newest-first. One test hiccup during this run, not a product bug: a freshly-inserted job's default `scheduled_for` briefly missed the drain route's `.lte(now)` filter due to a small clock offset between this local dev sandbox and the remote Supabase host — pinning the test job's `scheduled_for` to a safely-past fixed timestamp resolved it; irrelevant in real deployment, where the drain route and database run on well-synced infrastructure. All scratch data cleaned up afterward; the scratch route was deleted. |

| 3d | Rule-based insights engine v1 | **Done and verified end-to-end** (Smart alerts/Daily Pulse explicitly deferred — labeled a "Phase 3 stretch" in the spec itself, not silently dropped) | New `insights_snapshots` table (migration 0014, RLS gated on `analytics.view_all` — a business-wide gate, not `analytics.view_own`, since every rule here is inherently a tenant/location-wide fact, not scoped to one user's sales; no write policy, service-role only). `InsightsService.evaluateDailyInsights()` runs three deterministic rules, no LLM, over already-computed aggregates: **week-over-week change** (current Sunday-start week's closed-day gross sales vs. the prior week's, `"Sales increased/decreased by X%"`, severity `positive`/`warning`); **product concentration** (a product's revenue share of the current week's non-voided sales, flagged when `>30%`, using the **current** catalog name — same reasoning as 3b's product performance, not 3c's snapshot approach, since this is a business-wide weekly fact, not a historical record of one sale); **recurring low weekday** (last 8 weeks of closed days grouped by weekday, flags a weekday whose average is `<60%` of the overall average, requiring `>=3` samples for that weekday to avoid a single bad day producing a false pattern). Called from `app/api/cron/outbox/route.ts` right after a `daily_business_day_report` job's `generateDailyReport()` succeeds — same "a business day just closed" trigger as the report itself, wrapped in its own try/catch so an insights failure never undoes the report job's success (insights are a best-effort bonus riding the same event, not this job's core deliverable). New `features/analytics/components/insights-list.tsx` (icon + plain-language message per severity — no chart-color palette needed, each card is text + a status icon, not a data mark) rendered at the top of the Analytics page, gated on `analytics.view_all` to match the RLS policy. **Verified against the live project** via the established scratch-route pattern (6/6 assertions): a fixture with a +50% week-over-week increase, a 67%-share product, and 8 weeks of history where one weekday consistently underperforms produced exactly the 3 expected insights with the exact expected numbers in each message; a second run for the same business day didn't error (no dedupe logic exists yet — documented as current behavior, not a requirement); `listRecent()` returned the generated rows. All scratch data cleaned up afterward; the scratch route was deleted. |
| 3e | Corrections/void report | **Done and verified end-to-end** | Spec §38 lists "Sales Corrections" and "Void/Reversal Report" as two separate report types; combined into one `report_type: 'corrections_void'` here since both answer the same "what changed today" question — a deliberate simplification, not an oversight. `ReportService.generateCorrectionsReport()` reads `sale_corrections` (migration 0006's immutable ledger) for the business day's calendar date, filtered to the day's own `location_id` via `old_values->>location_id` (the jsonb snapshot `to_jsonb(sale)` already captured at void/correct time) rather than a second join back to `sales`. Skipped entirely (returns `null`, no row written) when nothing was voided/corrected that day — unlike the daily sales report, an empty corrections report every ordinary day would just be noise. Payload: void/correction counts, total voided amount, net corrected delta, and a per-entry list (sale number, product, reason, old/new amount, who requested it, resolved to the profile's name). Triggered from the same outbox handler as 3c/3d, in its own try/catch. `features/reports/components/report-list.tsx` now branches by `reportType` to render either the existing daily-sales card or a new corrections/void card. **Also fixes a real gap found while building this**: only the pg_cron sweep's auto-close path (migration 0011) ever queued a `report_jobs` row — `BusinessDayService.closeDay()` (the manual "Close" button, the far more commonly exercised path in testing so far) queued nothing at all, so daily reports/insights/corrections reports silently never generated for a manually-closed day. Fixed via `queue_daily_report_job()` (migration 0015, `SECURITY DEFINER`, gated by `is_tenant_member()`) since `report_jobs` has zero RLS policies and the RLS-respecting client `BusinessDayService` uses had no direct write path; `closeDay()` now calls it after every close, manual or automatic, best-effort (a failed enqueue doesn't fail the close itself). **Verified against the live project** via the established scratch-route pattern (9/9 assertions): the corrections report correctly totaled one void (40) and one correction (+10 delta) with the requester's name resolved, while a same-day correction at a *different* location was correctly excluded; a day with no corrections correctly produced no report row; `queue_daily_report_job()` correctly succeeded for a tenant member and was correctly rejected for a non-member of that business day's tenant; and — the key fix — actually calling `BusinessDayService.closeDay()` end-to-end through the RLS-respecting client confirmed a `report_jobs` row now gets queued where previously none would have. All scratch data cleaned up afterward; the scratch route was deleted. |

**Phase 3 is now fully done** (3a–3e) except weekly/monthly/custom report types, Resend email delivery + the notifications fan-out (deliberately deferred, see 2f/3c above), and Smart alerts/Daily Pulse (deferred, see 3d above).

## Phase 4 — Enterprise Controls (started: custom roles UI, users & invitations, security centre, MFA enrollment done)

| # | Milestone | Status | Notes |
|---|---|---|---|
| 4a | Custom roles UI | **Done and verified end-to-end** (WHICH user holds WHICH role is 4b's Users & Invitations screen, not this one — `docs/07-ui-ux-screen-map.md` folds "role assignment" into that screen; this increment is the role catalog itself: create/edit/delete a role and choose its permission grants) | The DB/RLS layer (`roles`/`role_permissions`/`user_role_assignments`/`permissions`, all gated on `roles.manage` — migration 0001) already existed and needed no changes; this increment is service + UI only. `PermissionService.listCatalog()` and `RoleService.{listRoles,getRole,createRole,updateRole,setRolePermissions,deleteRole}` (previously stubs/missing) are now real. `setRolePermissions` is a full replace (delete-then-insert), matching how the edit UI submits the whole desired checkbox state, not a diff. `deleteRole` is blocked for `is_system_default` roles (structural safety — nothing re-seeds them if removed) and for any role still holding a `user_role_assignments` row (both `role_permissions`/`user_role_assignments` cascade-delete with the role, which would otherwise silently strip an assigned user's access rather than surfacing it as a decision the admin has to make first). New `validations/role.ts`, `features/roles/{actions,components}/` (`RoleFormDialog` handles create and edit in one component — a `role` prop present means edit mode; permission checkboxes render as `Switch` toggles grouped by module, reusing the existing toggle component rather than introducing a new Checkbox primitive), `app/(tenant)/t/[tenantSlug]/(dashboard)/roles/page.tsx` (new, redirects non-`roles.manage` holders to More as a UX nicety — RLS is the actual enforcement regardless). More menu gained a conditional "Roles" entry (same pattern as Approvals — hidden outright, not shown-disabled, for anyone without the permission). **Verified against the live project** via the established scratch-route pattern (11/11 assertions): all 3 seeded default roles listed with correct permission counts (3/6/31) and correct `assignedUserCount`; a custom "Cashier" role was created with exactly the requested 2 permissions; rename + `setRolePermissions` correctly replaced (not merged) down to 1 permission; deleting a system-default role was correctly rejected; deleting a role still assigned to a user was correctly rejected, then correctly succeeded once unassigned; a staffer without `roles.manage` could still read the role list (RLS `roles_select` is membership-gated, not `roles.manage`-gated) but was correctly rejected attempting to create one. All scratch data cleaned up afterward; the scratch route was deleted. |

| 4b | Users & invitations | **Done and verified end-to-end** (invite-acceptance auto-flipping a membership from `invited` to `active` on first login not wired — an admin can flip it manually in the meantime, deliberately deferred rather than built speculatively) | `UserService.{inviteUser,setActive,setUserRole,listUsers}` (previously stubs) are now real. `inviteUser` is the one method in this increment that **must** run on the service-role client (added to `lib/supabase/service-role.ts`'s allowed-callers list) — checking whether an email already has a profile crosses tenant boundaries, which `profiles_select` RLS deliberately blocks for an ordinary admin's RLS-respecting client; the inviting admin's `users.create` grant is still checked first, via the RLS-respecting client, in the server action before the service-role step runs (same two-step pattern as `TenantService.createTenant`'s bootstrap). An existing email gets a new `tenant_memberships` row attached to their existing profile (no duplicate auth user, no email sent); a genuinely new email goes through `auth.admin.inviteUserByEmail()` (real Supabase invite email — subject to the same low-rate-limit default provider noted in Blocker #4). Re-inviting an already-a-member email is rejected. `setActive`/`setUserRole` run on the RLS-respecting client — `tenant_memberships_update` is gated on `users.edit` (the `users.disable` catalog permission isn't independently enforced by RLS, which only checks `users.edit` for every mutation on that table — the app layer deliberately mirrors that rather than implying a granularity RLS doesn't actually have). `setUserRole` is a full replace, same convention as `RoleService.setRolePermissions`. New `validations/user.ts`, `features/users/{actions,components}/` (`InviteUserDialog`, `UserList` with inline role `Select` + activate/disable button, self-disable hidden for the viewer's own row), `app/(tenant)/t/[tenantSlug]/(dashboard)/users/page.tsx`. More menu gained a conditional "Users" entry (gated on `users.create` or `users.edit`, same hidden-not-disabled convention as Approvals/Roles). **Verified against the live project** via the established scratch-route pattern (10/10 assertions, only one real invite email sent to stay within the rate limit): existing-profile invite correctly attached without creating a duplicate auth user; duplicate invite correctly rejected; a genuinely new email correctly created a new auth user with `status: 'invited'`; `listUsers` returned the correct roster (4 members), correct per-member role names, and correct `isCurrentUser` flagging; `setUserRole` correctly replaced (not added to) the staffer's role; `setActive` correctly disabled the staffer; and RLS correctly blocked the disabled staffer's own client from re-activating themselves (no `users.edit`). All scratch data cleaned up afterward; the scratch route was deleted. |

| 4c | Security centre — login_events + sessions | **Done and verified end-to-end** (admin-driven forced termination of a specific *other* live session is an explicit, documented gap — see below; working-hours restriction, geo-fencing/temporary-access, download security, and the full `audit_logs` coverage pass are separate, later roadmap items, not this one) | New `login_events`/`sessions` tables (migration 0016). Both have RLS with a SELECT policy (self, or a tenant member holding `security.manage`) but **zero write policies** — same posture as `report_jobs`/`insights_snapshots`, and not a coincidence: a *failed* login attempt has no authenticated session for a self-scoped insert policy to key off of, so both tables are written exclusively through the service-role client. `SecurityService` (new) added to `lib/supabase/service-role.ts`'s allowed-callers list for exactly this. `features/auth/actions/sign-in.ts` now logs every attempt (success and failure — a failure gets a best-effort `profile_id` looked up by email, purely for internal correlation, never exposed to the client, so repeated failures against one account are visible together) and creates a `sessions` row on success, plus a plain (non-secret, no auth authority of its own) `sid` cookie correlating "which row is this browser" for the security page's self-service action. New `lib/utils/user-agent.ts` (hand-rolled, best-effort browser/OS/device-type parsing — no new dependency for what the spec itself calls "best-effort, not a guaranteed physical-device fingerprint"; iPhone/iPad checked *before* the "Mac OS X" pattern, since every iOS UA string contains the legacy compatibility token "like Mac OS X" and would otherwise misclassify as desktop macOS). New Security page (always in the More menu, unlike the permission-gated Approvals/Roles/Users entries — every signed-in user manages their own sessions regardless of permissions) shows the caller's own sessions + recent sign-ins, plus a tenant-wide sign-in activity section that only renders for `security.manage` holders. Self-service "Sign out of other devices" is fully real (`auth.signOut({ scope: 'others' })`, a genuine per-caller Supabase capability) and also marks the other `sessions` rows revoked in our tracking table. **Documented, not silently glossed over**: `revokeOtherSessions`/an admin "revoke" action mark our own tracking rows as revoked (a real, useful audit trail) but do **not** forcibly invalidate another user's live Supabase JWT elsewhere — the supabase-js admin API only exposes `admin.signOut(jwt)`, which needs the actual token, not a session ID, and this project's own hard rule (docs/05) forbids touching `auth.*` tables directly to work around that; `UserService.setActive(false)` (Phase 4b) remains the mechanism that actually blocks a disabled user's access reliably. **Verified against the live project** via the established scratch-route pattern (13/13 assertions): UA parsing correct for Chrome/Windows and Safari/iOS (including the Mac-OS-X-token edge case); `login_events` correctly stores `device`/`browser`/`os` as separate columns; a user correctly read their own login events/sessions; the `security.manage` holder correctly saw tenant-wide events including another user's; RLS correctly filtered a non-`security.manage` staffer down to zero rows when explicitly querying someone else's `profile_id`, and down to *only their own* row (not the owner's) in the tenant-wide view — since RLS's "mine OR security.manage" clause independently admits a caller's own row either way; `revokeOtherSessions` correctly left an excluded (current) session untouched while revoking the other. All scratch data cleaned up afterward; the scratch route was deleted. |

| 4d | MFA enrollment | **Done and verified end-to-end** (enrollment/disablement only — *enforcement* as a requirement for business-day reopen / `security.manage`-gated actions / platform-admin impersonation is a separate follow-up now that there's something to require, per docs/05 and 2h's already-documented deferral; MFA stays fully optional everywhere today) | TOTP via Supabase Auth (`supabase.auth.mfa.*`) — no new tables or migration, since Supabase manages `auth.mfa_factors` internally and this project's own hard rule (docs/05) forbids reading that schema directly regardless. New `features/security/components/mfa-enrollment.tsx` calls the MFA API directly on the browser client (`lib/supabase/client.ts`), same precedent as `product-image-upload.tsx`'s direct Storage calls — a pure client-SDK passthrough against the caller's own session, no service-role privilege or server-side DB write at any step, so no service wrapper was introduced for it. Flow: enroll → show QR + manual secret → verify with a 6-digit code (`challengeAndVerify`, promotes the session to `aal2`) → enrolled. An abandoned enrollment (QR shown, never verified) is cleaned up via `unenroll` on Cancel rather than left as a stray unverified factor. Disabling always requires a fresh step-up code (`challengeAndVerify` again, immediately before `unenroll`) — both because Supabase can require `aal2` for unenrolling a verified factor, and because it's the right UX for removing a security control regardless of whether the API strictly demands it. Rendered as a new card at the top of the existing Security page (4c) rather than a separate route. **Verified against the live project** via the established scratch-route pattern (11/11 assertions) — genuinely end-to-end, not just "the call didn't error": a real RFC 6238 TOTP generator (hand-rolled in the scratch route, base32-decode + HMAC-SHA1 + dynamic truncation) computed actual valid codes from the secret Supabase returned, and Supabase's real server-side verification accepted them (`usedOffset: 0`, no clock-tolerance fallback needed); an incorrect code was correctly rejected; `listFactors()`'s per-type arrays (`data.totp`) were confirmed to only ever contain **verified** factors per the SDK's own types — an unverified factor only shows up in `data.all`, which the test caught and adjusted for rather than asserting against the wrong shape; the session's AAL was confirmed promoted to `aal2` after verification; the full disable flow (step-up + `unenroll`) correctly worked and left `listFactors()` empty again. All test artifacts cleaned up afterward; the scratch route was deleted. |

| 4e | Working-hours login restriction | **Done and verified end-to-end** (login-time blocking only — the in-session "countdown warning, then sign out" half described in docs/05 is a client-side polling/timer concern, deliberately deferred rather than built speculatively alongside the server-verifiable half; geo-fencing, download security, and the full `audit_logs` coverage pass remain separate, later Phase 4 items) | New generic `TenantService.{getSetting,setSetting}<T>` (previously stubs) on the existing `tenant_settings` key/value table — no migration needed. New `AuthService.evaluateAccessGate({ tenantId })`, the single access-gate composition point docs/05 calls for (geo-fencing and tenant-suspension checks are meant to land in this same gate later, not a redesign). Resolves the tenant's first location (Phase 1's one-location-per-tenant reality), reads the `restrict_login_to_working_hours` setting, resolves today's hours via `special_hours` (date-specific override) then `location_hours` (weekly default), and checks whether "now" (in the location's timezone) falls inside the window. Times are compared as minutes-since-midnight integers, not strings — a naive lexicographic compare breaks for an overnight window (e.g. open 22:00, close 02:00), same overnight-wrap reasoning `run_business_day_sweep()` already applies in SQL. Fails open (allows sign-in) when the setting is off, no location exists, or no hours are configured for today at all — only an explicit `closed_all_day`/`is_closed` or a real window the current time falls outside blocks. `features/auth/actions/sign-in.ts` now calls the gate after resolving the active tenant; a blocked attempt tears down the session `signInWithPassword` already created (`supabase.auth.signOut()`), logs a failed `login_events` row with the gate's reason, and returns `{ error }` instead of redirecting. New `features/security/actions/set-working-hours-restriction.ts` (gated on `settings.manage`) and `features/security/components/working-hours-restriction-toggle.tsx` (optimistic `Switch`, rollback on error), rendered on the existing Security page (4c) alongside the new `settings.manage` fetch. Fixed a real type bug along the way: `tenant_settings`'s Insert type in `database.types.ts` required the full Row (including `updated_at`), inconsistent with every other table's convention — narrowed to `Partial<Row> & { tenant_id, setting_key, value }`. **Verified against the live project** via the established scratch-route pattern (10/10 assertions, one bug caught and fixed by the suite before shipping): `getSetting`/`setSetting` round-tripped correctly; gate correctly allowed when the setting is off, when no hours were configured for today, and when now was genuinely within a configured window; gate correctly blocked (with the window in the reason) when now was outside a configured window, and when `special_hours` marked the location closed today despite an otherwise-open weekly window; a full `signInAction` integration test against a blocked tenant confirmed it returns an error (not a redirect), creates no `sessions` row, and writes exactly one `login_events` row marked failed with the gate's reason. The "within configured hours" case initially failed — the test's randomly-generated window happened to straddle midnight UTC, exposing the string-comparison bug above before any real user could hit it; fixed and re-verified 10/10. All scratch data cleaned up afterward; the scratch route was deleted. |

| 4f | Geo-fencing + temporary-access-request | **Done and verified end-to-end** (login-time blocking only, same scoping as 4e; the in-session countdown/expiry-warning half is a separate, deliberately deferred client-timer concern; download security and the full `audit_logs` coverage pass remain later Phase 4 items) | `locations.lat`/`long`/`geofence_radius_m` already existed (migration 0001) — the only schema addition is migration 0017's `temporary_access_requests` table (RLS: self or `approvals.manage`, zero write policies — written exclusively by SECURITY DEFINER functions, same posture as `approval_requests`). `AuthService.evaluateAccessGate` gained a second, independent check in the SAME composition point 4e established (`restrict_login_to_geofence` tenant setting): resolves the tenant's primary location's fence, and — unless the caller already holds an active approved grant (checked first, bypassing the fence entirely for its window) — blocks when coordinates are missing (denied/unavailable) or outside the radius (haversine distance, new `lib/utils/geo.ts`), both with the same `blockedBy: "geofence"` reason so the client always knows to offer the escape hatch. **Reuses the Approval Engine as its own spec calls for** ("the Approval Engine's second consumer"): new `request_temporary_access()` SECURITY DEFINER function creates an `approval_requests` row (type `temporary_location_access`) plus a `temporary_access_requests` row — always pending, no auto-approve path (unlike void/correct/reopen, the spec never describes one for this type); `resolve_approval_request()` gained a fourth dispatch branch that grants (`granted_until = now() + requested_duration_minutes`, capped at 12h) on approval, and the shared "rejected" path now also flips this type's own tracking row (the other three types have no equivalent secondary row to flip). `run_business_day_sweep()` (the existing pg_cron job, already scheduled — no new cron job needed since it's the same function definition) gained one more pass expiring stale-but-still-'approved' grants; the gate's own `granted_until > now()` check already makes this correctness-independent, so the pass is audit-trail accuracy, not load-bearing. Login flow: `request_temporary_access` must run under a live authenticated session (keys off `auth.uid()`), so it's only reachable from a new, narrow window — `features/auth/actions/request-temporary-access.ts` re-authenticates independently (the email/password the user already typed on the blocked login attempt, not re-prompted) inside its own sign-in→request→sign-out lifecycle, mirroring `sign-in.ts`'s own gate-check lifecycle rather than inventing a new one. `login-form.tsx` does a best-effort, 5s-timeout `navigator.geolocation` read before every submit (resolves to nulls on denial/unavailability/timeout, never blocks the submit) and renders `RequestTemporaryAccessForm` (reason + duration) only when `signInAction` reports `blockedBy: "geofence"`. New `GeofenceRestrictionCard` on the Security page (alongside 4e's working-hours toggle) combines the on/off toggle with lat/long/radius inputs and a "Use my current location" convenience button (new `TenantService.{setLocationGeofence,getPrimaryLocationGeofence}`) — without this, the restriction toggle would have had no way to actually configure a fence, since no other screen edits `locations` post-onboarding. `ApprovalsInboxList`'s type-label map gained `temporary_location_access`; `business_day_reopen` was also added as a drive-by fix (same file, same missing-label gap, unrelated to this feature but trivial to fix while touching the map). **Verified against the live project** via the established scratch-route pattern (21/21 assertions, genuinely end-to-end): gate composition correct (setting off → allowed regardless of location; setting on with no fence configured → fails open; within radius → allowed; missing coordinates and outside-radius → both blocked with `blockedBy: "geofence"`); a full `signInAction` integration test confirmed a geofence-blocked attempt returns an error with `blockedBy` set and creates no `sessions` row; a full `requestTemporaryAccessAction` integration test confirmed it re-authenticates, creates the request pending, and signs back out (still no `sessions` row); the request was approved through the exact same generic `ApprovalService.resolve()` the Approvals page UI calls, running under a real signed-in Tenant Administrator session (not service-role) to prove real RLS `approvals.manage` behavior, not a bypassed one; the grant correctly bypassed the fence with no coordinates at all; a second real `signInAction` attempt correctly redirected (succeeded) and created a real `sessions` row; a second, separate request was correctly rejected and its tracking row flipped to `rejected`; backdating a grant's `granted_until` and invoking `run_business_day_sweep()` directly confirmed it flips to `expired` and the gate immediately stops honoring it. All scratch data cleaned up afterward; the scratch route was deleted. |

| 4g | Download security | **Done and verified end-to-end** (applied to the one export surface that actually exists today — Sales History's CSV button; there's no analytics/audit export built yet to apply it to, so "sales/transaction/analytics/audit exports" per the spec's own wording is scoped to what currently exists, not built speculatively ahead of those features) | `require_download_passcode` and `hashed_download_passcode` live in the existing `tenant_settings` table, same convention as 4e/4f's `restrict_login_to_*` settings — no migration needed for either. New migration 0018: `download_audit` table (`id, tenant_id, profile_id, export_type, entity_ref, passcode_verified_at, ip, created_at` per `docs/03-database-schema.md`). Unlike `login_events`, an export only ever happens under an already-authenticated session, so a plain self-scoped RLS INSERT policy (`profile_id = auth.uid()`) is enough — no service-role/SECURITY DEFINER detour needed. New `lib/utils/passcode.ts` (`hashPasscode`/`verifyPasscodeHash`, scrypt via Node's built-in `crypto` — no new dependency for one tenant-wide passcode, same hand-rolled-over-a-library precedent as this session's RFC 6238 TOTP work) and `TenantService.setDownloadPasscode` (thin wrapper so callers never handle the plaintext passcode past one call). New `DownloadService` owns exactly the "Validate" and "download_audit event" steps of the spec's flow (Enter Passcode → Validate → Permission Check → Generate File → `download_audit` event) — the caller owns the permission check and file generation, since those are specific to whatever's being exported. Sales History's CSV export (previously a pure client-side `Blob` build over whatever rows were already rendered on the page) moved server-side into new `features/sales/actions/export-sales-history.ts`: checks `sales.view_all`/`sales.view_own`, validates the passcode when required, re-runs `SalesService.listRecent()` with the same filters the page itself used (not the client's already-rendered array), builds the CSV (new generic `lib/utils/csv.ts`), and logs the audit row — a rejected passcode attempt returns before any of that, so (per the spec's own step order) only successful exports get logged. `sale-history-list.tsx`'s export button now prompts for a passcode via a small dialog only when the tenant requires one. New `DownloadSecurityCard` on the Security page (alongside 4e/4f's toggles) combines the requirement toggle with a set/change-passcode form — the passcode itself is never displayed back, only "configured" or not, matching the password-visibility boundary every other secret in this app follows. **Verified against the live project** via the established scratch-route pattern (9/9 assertions, one bug caught and fixed before shipping): `hashPasscode`/`verifyPasscodeHash` round-tripped correctly and rejected a wrong passcode; export succeeded without a passcode when the requirement is off; a tenant with the requirement on but no passcode ever configured correctly rejected every attempt; a wrong passcode was rejected and correctly logged **no** `download_audit` row; the correct passcode succeeded, produced a real CSV with an actual seeded sale's data (not just a header), and logged exactly one audit row with `passcode_verified_at` set; a tenant member with no role assignment at all was correctly rejected on the permission check even with the right passcode. The scratch test itself hit a real bug in its own setup (not app code): the `sales.idempotency_key` column is `uuid`, not `text` — a `Date.now()`-based string seed value failed Postgres's uuid-format check; fixed by seeding a real `crypto.randomUUID()`. All scratch data cleaned up afterward; the scratch route was deleted. |

| 4h | Full `audit_logs` coverage pass | **Done and verified end-to-end** (every event type with a real corresponding feature is wired; `SUBSCRIPTION_CHANGED` and `IMPERSONATION_STARTED/ENDED` are deliberately not wired anywhere — Billing/Paystack (Phase 6) and Platform Administration/impersonation (Phase 7) don't exist yet, so there is nothing to log; when either phase lands, its own action is the place to add the call) | New migration 0019: `audit_logs` table (`id, tenant_id, actor_profile_id, action, entity_type, entity_id, old_values, new_values, reason, ip, device, metadata, created_at` per `docs/03-database-schema.md`), zero write policies — same posture and reasoning as `login_events` (a `FAILED_LOGIN` has no authenticated session for a self-scoped policy to key off of), so every event type writes through `AuditService.log()` on the service-role client rather than switching client depending on event type. `security.manage`-gated SELECT, reusing the same permission that already gates tenant-wide `login_events` visibility rather than inventing a new one — no viewer UI was built in this pass (the roadmap item is coverage/instrumentation, not a new screen). `AuditService` (previously a stub) is now real; new `lib/audit/actions.ts` centralizes the ~23-entry event vocabulary as a shared const so ~25 call sites use identical strings. Every caller treats a logging failure as best-effort (`.catch(() => {})`) — an audit write must never block the real mutation it's recording. Wired at the action layer: `LOGIN`/`LOGOUT`/`FAILED_LOGIN` (sign-in.ts/sign-out.ts — a wrong-password failure logs with `tenant_id: null`, same as its `login_events` counterpart, since it happens before tenant resolution), `SALE_CREATED` (record-sale.ts, skipped for idempotent replays), `SALE_VOIDED`/`SALE_EDITED` (void-sale.ts/correct-sale.ts for the immediate/auto-approved path), `BUSINESS_DAY_OPENED`/`BUSINESS_DAY_REOPENED` (open-business-day.ts/reopen-business-day.ts), `PRODUCT_CREATED`/`PRODUCT_EDITED`, `USER_INVITED`/`USER_DISABLED`/`USER_ENABLED`, `ROLE_CHANGED` (a membership's role assignment), `PERMISSION_CHANGED` (a role's own permission grants — create/update/delete-role.ts all three, since deleting a role is as much a permission-configuration change as editing one), `SECURITY_SETTING_CHANGED` (all 5 of 4e/4f/4g's settings toggles — the passcode-value action logs only that it changed, never the passcode itself, same log-redaction rule as every other secret in this app), `EXPORT_REQUESTED`/`EXPORT_COMPLETED` (export-sales-history.ts — REQUESTED logs right after the permission check so a passcode-rejected attempt still leaves a trace, COMPLETED only after the CSV is actually generated), `TEMPORARY_ACCESS_REQUESTED` (request-temporary-access.ts). `resolve-approval.ts` is the one dispatch point needing a lookup-before-resolve: `ApprovalService.resolve()`'s rejected branch doesn't return the request's `type`, so the action fetches `approval_requests.type` first and maps it to the matching audit action (`SALE_VOIDED`/`SALE_EDITED`/`BUSINESS_DAY_REOPENED`/`TEMPORARY_ACCESS_APPROVED` on approval, `TEMPORARY_ACCESS_REJECTED` only for that one type on rejection, matching the doc's vocabulary) — this is also where an approval-GATED mutation (a tenant that requires review) actually applies, complementing the immediate-path logging already in the originating actions, so the two never double-log the same event. `BUSINESS_DAY_OPENED`/`BUSINESS_DAY_CLOSED` also happen automatically with no app-layer caller at all (the only real "close" path today is the pg_cron sweep and the lazy auto-relock check) — `run_business_day_sweep()` and `auto_relock_expired_business_day()` (both already-scheduled/already-called, no new cron job) gained inline `audit_logs` inserts with `actor_profile_id: null` (system actor), a full create-or-replace of both functions with the scheduling/relock logic itself byte-for-byte unchanged from migration 0017. **Verified against the live project** via the established scratch-route pattern (42/42 assertions across one long, realistic session as a real bootstrapped Tenant Administrator): every one of the ~20 wired event types confirmed to write a correctly-shaped row (right tenant, right actor, right entity) for opening a business day, creating/editing a product, recording two sales then voiding one and correcting the other, reopening a closed day, an automatic relock-driven close logged with a null system actor, inviting/disabling/re-enabling a user and changing their role, creating/updating/deleting a custom role (3 `PERMISSION_CHANGED` rows for one role, confirmed by count), toggling a security setting, a full export (both `EXPORT_REQUESTED` and `EXPORT_COMPLETED`), logout then a real login (both via the actual redirect-throwing `signInAction`/`signOutAction`, not bypassed), a wrong-password failure (confirmed logged with `tenant_id: null`), and a full temporary-access request → approve → grant, plus a second request → reject, both resolved through the exact same generic `ApprovalService.resolve()` the Approvals page calls. Two real bugs were caught in the test's own setup (not app code) and fixed before completing: `createProductSchema`'s `imageUrl` field requires `""` not absent/`null` (same class of Zod-vs-`FormData.get()` gotcha as everywhere else in this app), and `resolveApprovalSchema`'s `notes` field the same way; a third finding was in Supabase itself, not this app: `auth.admin.inviteUserByEmail()` rejects `@example.com` for a real send (stricter than `admin.createUser()`), worked around by exercising `inviteUserAction`'s existing-profile branch instead of re-proving email delivery already verified in 4b. All scratch data cleaned up afterward; the scratch route was deleted. |

**Phase 4 — Enterprise Controls is now fully done** (4a–4h).

## Phase 5 — Data Migration (5a done: historical sales import, end-to-end)

| # | Milestone | Status | Notes |
|---|---|---|---|
| 5a | Historical sales import: template generation → validation engine → preview/resolve-errors → confirm/import → analytics rebuild trigger | **Done and verified end-to-end** (scoped to `type = 'sales_history'` — `imports`/`import_rows` (migration 0020) are shaped generically enough for `type = 'products'` too, per `docs/12-imports-data-migration.md`'s own "same shape" framing, but that template/validation/confirm logic is a deliberate, documented follow-up, not built speculatively alongside sales history) | New dependency `exceljs` (template generation + `.xlsx`/`.csv` parsing) — picked over the more common `xlsx`/SheetJS package specifically to avoid its known CVE history; `exceljs`'s own `uuid` sub-dependency carries one moderate advisory (a buffer-bounds-check issue in `uuid` v3/v5/v6 when an explicit buffer is passed), noted here rather than silently ignored — not practically exploitable through this app's usage (we never pass user-controlled buffers into `uuid` generation). New migration 0020: `imports.manage` permission, `imports`/`import_rows` tables (RLS: `imports.manage`-gated SELECT, **zero write policies** — writes always go through the service-role client, since confirming an import needs to bypass the live-capture-shaped `business_day.open`/`sales.create` RLS checks entirely to correctly create a historical, already-`closed` business day), and a private `imports` Storage bucket (never publicly fetchable, no `storage.objects` policy needed since only the service-role client ever touches it). New `ImportService` (previously a stub): `generateSalesHistoryTemplate()` builds a real downloadable `.xlsx` (data sheet + a separate Instructions sheet); `createImport`/`validateImport` upload the file to Storage, parse it, and validate every row against tenant lookups (products/locations/active members) fetched **once** up front, not per row; `resolveRow` re-runs the exact same row-validator for an on-screen fix as the bulk pass used; `confirmImport` only processes currently-`valid` rows (rows still `invalid` are left untouched, not force-skipped, so a partial import is always safe to confirm now and finish resolving later), groups touched (location, date) pairs into historical `business_days` rows (`status: 'closed'`, `closing_reason: 'Historical import'` — never `'open'`, since backfill isn't a live capture window), and inserts `sales` rows reusing the exact same table/triggers/snapshot-field/idempotency conventions live-captured sales use (`assign_sale_number`, the `(tenant_id, idempotency_key)` uniqueness constraint) — "the same insert path as live-captured sales" the doc calls for, at the level that actually matters, not a literal call to `SalesService.recordSale()` (whose "business day must be open" gate is a live-capture UX guard that doesn't apply to historical backfill). Each row's own `import_rows.id` is used as its sale's `idempotency_key`, so a retried confirm can never double-insert — and the retry-lookup path correctly resolves the **existing** sale's own generated id on a conflict rather than misusing the row id itself (a real bug caught and fixed during review, before it ever ran live). New pure, DB-free `lib/imports/row-validation.ts` (`validateSalesHistoryRow`) checks every rule the doc lists (required fields, date/number format, negative amounts, unknown products/locations/sales-people, in-batch duplicate "Existing Reference" values, no-future-dates) and collects every error on a row at once, not just the first, matching the doc's "Row 24 — Product not found" per-row-reason example. **Analytics rebuild trigger**: `AnalyticsService`/product analytics already compute live from raw `sales`/`business_days` rows (no cache to invalidate, confirmed by re-reading that service's own header comment), so nothing was needed there; the one real cached artifact is `insights_snapshots` (Phase 3's rule-based engine) — `confirmImport` calls `InsightsService.evaluateDailyInsights()` **directly and synchronously** for every touched historical business day (not queued through the `report_jobs` outbox, since that only drains once a day on this project's Hobby-plan Vercel Cron, and "immediately appears in dashboards" is the whole point of this step); a `report_jobs` row is still queued per touched day for the daily report artifact itself, matching what a live business-day close already does. New `app/api/t/[tenantSlug]/imports/template/route.ts` (permission-gated `.xlsx` download, not passcode-gated the way 4g's exports are — a blank template has no real tenant data in it) and a full UI: `app/(tenant)/.../imports/page.tsx` (upload + past-imports list) and `.../imports/[importId]/page.tsx` (the preview/resolve/confirm screen — a table of every row with per-row errors, an on-screen "Fix row" dialog for the ten template fields, and a "Confirm Import" button). Imports doubles as its own audit trail per the doc's own design (`uploaded_by`/`confirmed_by`/`confirmed_at`/row counts on the `imports` row itself), so this wasn't also wired into 4h's `audit_logs` vocabulary, which has no `IMPORT_*` event type. New conditional "Imports" entry in the More menu, gated on `imports.manage` (same hidden-not-disabled convention as Approvals/Roles/Users). **Two real bugs were caught during a careful self-review pass before ever running this live** (not user-reported): (1) the template generator originally appended `" *"` to required column headers for a visual cue, which the parser's exact-header-text matching would never have recognized — its own generated template wouldn't have parsed correctly; fixed by keeping header text byte-for-byte plain and moving the required/optional cue to the Instructions sheet. (2) `confirmImport`'s idempotency-conflict path originally fell back to using the `import_rows.id` itself as the resolved sale id on a unique-constraint conflict — wrong, since `sales.id` is its own independently generated uuid; fixed to look up the existing sale by `(tenant_id, idempotency_key)`, the same idempotent-replay pattern `SalesService.recordSale()` already uses. **Verified against the live project** via the established scratch-route pattern (29/29 assertions, genuinely end-to-end): the generated template round-tripped through the exact same parser real uploads use, confirming bug (1) is actually fixed; a real re-upload of the template's own unmodified example row (the realistic "user left the example row in" case) validated and imported correctly; an 11-row file exercised every validation rule at once (missing amount, unknown product, future date, negative amount, in-batch duplicate reference, bad date format, unknown sales person, unknown location) with the correct specific reason on each row; an on-screen resolve correctly flipped a row from invalid to valid and updated the parent import's live counts; confirming created three separate historical `business_days` rows across two locations and two different dates with exactly correct aggregates on each; all four confirmed sales correctly referenced the real product and one was correctly attributed to a specific team member resolved by email; a second, redundant confirm call was confirmed to be a safe no-op (no error, no duplicate sales); three `report_jobs` rows were queued, one per touched historical day; and a tenant member with no `imports.manage` grant was correctly rejected. All scratch data cleaned up afterward; the scratch route was deleted. |

Not yet started: products bulk upload (`type = 'products'`, same `imports`/`import_rows` schema, deliberately deferred per 5a's own scoping note above). Phases 6–7 not started. See `01-development-roadmap.md` for the full granular milestone breakdown.

## What actually exists right now

- Full Next.js 15.5.23 + TypeScript strict + Tailwind v4 + shadcn/ui scaffold, builds and lints clean.
- Complete `/docs` suite (this file plus 20 others) reflecting the architecture decisions made during planning.
- `supabase/migrations/0001_core_tenancy_and_rbac.sql` — schema + RLS + permission catalog seed, applied and verified live.
- `lib/supabase/{client,server,service-role}.ts` — the three-client security boundary, fully implemented.
- `lib/permissions/can.ts` — implemented against the migration's SQL functions.
- `types/database.types.ts` — hand-written, PROVISIONAL, matches migration 0001 exactly (including the `Relationships: []` field every table needs for Supabase's generic type inference to work — this was a real gotcha during setup, see below) and confirmed accurate against the live schema by the spot-verification above. Regenerating it from the CLI (`supabase gen types typescript --project-id bajjwqlrxzamvdwjzxdq`) requires a Supabase personal access token (`supabase login` or `SUPABASE_ACCESS_TOKEN`), which hasn't been provided — not currently blocking anything since the hand-written version is verified accurate, but worth doing for exact fidelity once convenient.
- 14 service stub files remain stubs; 3 are now real implementations verified against the live database: `AuthService` (sign up/in/out, password reset), `TenantService` (`createTenant` bootstrap), `RoleService` (`seedDefaultRoles`).
- `.env.local` populated with the real Supabase URL/anon/service-role keys (gitignored, not committed).
- `middleware.ts` + `lib/supabase/middleware.ts` — Supabase session-refresh middleware.
- `validations/auth.ts` — Zod schemas (sign-up, login, password-reset request/confirm) shared by both the React Hook Form resolvers and the server actions that parse `FormData`.
- `features/auth/` — sign-up/login/logout/password-reset server actions (`actions/`) and their RHF-driven form components (`components/`).
- `app/(auth)/{signup,login,verify-email,reset-password,reset-password/confirm}` pages, `app/api/auth/callback/route.ts`, `app/(tenant)/t/[tenantSlug]/{layout,page}.tsx` (minimal placeholder — real Capture Sales screen is Phase 2d), `app/no-tenant/page.tsx`, and `app/page.tsx` rewritten to route signed-in/out users correctly instead of the default Next.js starter page.
- `supabase/migrations/0002_add_tenant_country.sql` and `0003_profile_phone_from_signup.sql` — both applied and verified live (see Decisions below).
- `hooks/tenant-context.tsx` — `TenantProvider`/`useTenant()`/`usePermission()`, wired into the tenant layout; verified against the live project by logging into the real running app and confirming a permission-gated element (`features/sales/components/sales-visibility-badge.tsx`) rendered correctly for a seeded Tenant Administrator.
- Brand mark: `app/icon.svg`, `app/favicon.ico`, `app/apple-icon.png`, `components/shared/logo.tsx`, `public/logo.svg` — see Decisions below.
- `main` and `development` are in sync (fast-forward merged) as of this entry; going forward, keep pushing them together or merge promptly rather than letting `development` drift far ahead, since Vercel's Production deployment tracks `main`.
- Global typography switched to Google Outfit (self-hosted via `next/font/google`) and the default page background to warm cream — see `07-ui-ux-screen-map.md`'s new Typography & colour section for the exact tokens. Verified against the actual served output (not just "should work"): fetched the running dev server's compiled CSS and confirmed both `font-family: 'Outfit', 'Outfit Fallback'` wired to `--font-sans` and `--background: #faf3e6` / `#1b1611` (dark) present in the real bundle.
- `app/manifest.ts`, `public/sw.js`, `components/shared/{service-worker-registration,bottom-nav}.tsx` — PWA installability + persistent nav, see 1f above.
- Tenant routes restructured under `(dashboard)`: `sales/`, `analytics/`, `reports/`, `more/` placeholder pages, `(dashboard)/layout.tsx` (renders the bottom nav), `/t/[slug]/page.tsx` now just redirects to `/sales`. All sign-in/sign-up/root-page redirect targets updated to land directly on `/sales` instead of the bare tenant root.
- `features/onboarding/` — 7-step wizard (`components/`), server actions for the two real steps (`actions/save-business-details.ts`, `actions/save-location-hours.ts`), `validations/onboarding.ts`. `TenantService` gained `updateBusinessDetails`/`upsertPrimaryLocation`/`setLocationHours`. `lib/tenant/resolve-active-tenant.ts` renamed from `resolveActiveTenantSlug` to `resolveActiveTenant`, now returns `{ slug, needsOnboarding }`.
- `types/database.types.ts` gained the `location_hours` table (previously omitted from the provisional types for brevity, needed once the wizard started writing to it).
- `supabase/migrations/0004_platform_admin_foundation.sql` and `0005_sales_engine_core.sql` — both applied and verified live (see 1g/Phase 2 above). `supabase/seeds/001_platform_admin_bootstrap.sql` — run manually whenever convenient, idempotent.
- `types/database.types.ts` gained `platform_admins`, `products`, `product_images`, `business_days`, `sales` table entries and the `PlatformAdminRole`/`ProductStatus`/`BusinessDayStatus`/`SaleStatus` type aliases.
- `PlatformAdminService`, `ProductService`, `BusinessDayService`, `SalesService` are now real implementations (previously stubs) — see Phase 1g/2 rows above for exactly which methods.
- `app/(platform-admin)/admin/{layout,page}.tsx` — platform-admin shell + guard + KPI dashboard.
- `app/(tenant)/t/[tenantSlug]/(dashboard)/products/page.tsx`, `features/products/`, `validations/product.ts` — product catalog CRUD (create/archive; list is read-only for now).
- `features/sales/{actions,components}/`, `validations/sale.ts` — the real Sales Capture screen replacing the Phase 1f placeholder: business-day gate, product grid, record-sale bottom sheet, idempotent submission, success toast.
- `app/layout.tsx` now mounts `<Toaster />` (was installed but never rendered anywhere until this increment — see Decisions below).
- `supabase/migrations/0006_approval_engine_and_sale_corrections.sql` — applied and verified live (see 2e/2g above). Adds `approval_requests`, `sale_corrections`, the `approvals.manage` permission, and the `void_sale`/`correct_sale`/`resolve_approval_request` `SECURITY DEFINER` functions (plus two never-client-callable internal helpers, `_apply_sale_void`/`_apply_sale_correction`).
- `types/database.types.ts` gained `approval_requests`/`sale_corrections` table entries, the `void_sale`/`correct_sale`/`resolve_approval_request` `Functions` entries, and `ApprovalRequestStatus`/`SaleCorrectionType`/`VoidOrCorrectResult`/`ResolveApprovalResult` types.
- `ApprovalService` (previously a stub) is now real: `listPending`/`resolve`. `SalesService` gained real `voidSale`/`correctSale`/`listRecent` (voidSale was previously a stub).
- `validations/sale.ts` gained `voidSaleSchema`/`correctSaleSchema`/`resolveApprovalSchema` (server-action-only, safe to use `z.coerce` since none are RHF-bound).
- `features/sales/actions/{void-sale,correct-sale,resolve-approval}.ts`, `features/sales/components/{void-sale-dialog,correct-sale-dialog,sale-history-list,approvals-inbox-list,approval-row-actions}.tsx` — the void/correct/approve/reject UI, all plain `useState`-driven dialogs (no RHF), matching `record-sale-sheet.tsx`'s existing pattern.
- `app/(tenant)/t/[tenantSlug]/(dashboard)/sales-history/page.tsx` and `.../approvals/page.tsx` — new real pages, both wired into the More menu (Sales History was previously a disabled placeholder; Approvals is new and permission-conditional).
- `supabase/migrations/0007_product_images_storage.sql` and `0008_product_images_storage_select_policy.sql` — applied and verified live (see 2a above). 0007 creates the public `product-images` bucket + insert/update/delete RLS on `storage.objects` (gated by `products.edit`, scoped by the tenant-id folder segment); 0008 fixes a gap 0007 shipped with — see Decisions.
- `ProductService` gained `update`, `listAll`, `setStatus`, `setImage`, `removeImage`; `Product`/`CreateProductInput` gained `description`; `CreateProductInput` gained optional `id`/`imageStoragePath` so a client-generated product id can be used as the Storage path prefix before the row exists.
- `validations/product.ts` gained `updateProductSchema`/`setProductStatusSchema`/`setProductImageSchema`.
- `features/products/actions/{update-product,set-product-status,set-product-image,remove-product-image}.ts` — new server actions, all `assertCan`-gated (`products.edit` or `products.archive`) in addition to the RLS the DB already enforces, matching `lib/permissions/can.ts`'s "RLS proves access, app layer gives a good error message" pattern. `create-product.ts`/`archive-product.ts` gained the same `assertCan` calls (previously relied on RLS alone).
- `features/products/components/{product-image-upload,edit-product-dialog,product-management-list}.tsx` — new. `product-image-upload.tsx` is Storage-only (no DB writes) and reusable between the create form (defers persistence until the product itself is created) and the edit dialog (persists immediately via `set-product-image`/`remove-product-image`).
- `supabase/migrations/0009_business_day_reopen.sql` and `0010_product_photo_view_name_toggle.sql` — applied and verified live (see 2h above). `features/products/components/product-photo-viewer.tsx` (shared square thumbnail + lightbox, used by both the sales grid and product management list), `features/sales/{actions/reopen-business-day.ts,components/reopen-business-day-dialog.tsx}`, `validations/business-day.ts`.
- `supabase/migrations/0011_business_day_sweep_and_report_jobs.sql` — applied and verified live (see 2f above). `app/api/cron/outbox/route.ts` + `vercel.json` (Vercel Cron, `*/5 * * * *`, `CRON_SECRET` bearer-auth) drain the new `report_jobs` outbox table via the service-role client.
- `types/database.types.ts` gained `report_jobs`, `special_hours` (existed in migration 0001 but was missing from the provisional types until now), `run_business_day_sweep`, `ReportJobStatus`, `BusinessDaySweepResult`.
- `SalesService.listRecent()` gained `dateFrom`/`dateTo`/`search`; `features/sales/components/sale-history-filters.tsx` (new, URL-search-param-driven) and `sale-history-list.tsx`'s client-side CSV export (see 2i above).
- `supabase/migrations/0012_fix_sale_number_uniqueness_scope.sql` — written, not yet applied live (see 2f-bug above).
- `AnalyticsService` (previously a stub) is now real: `getKpis`/`getProductPerformance` (see 3b above). `lib/utils/date-ranges.ts` (new), `features/analytics/components/{analytics-filters,kpi-cards,product-performance-list}.tsx` (new), `app/(tenant)/t/[tenantSlug]/(dashboard)/analytics/page.tsx` (previously a placeholder).
- `supabase/migrations/0013_reports_table.sql` — applied and verified live (see 3c above). `ReportService` (previously a stub) is now real: `generateDailyReport`/`listReports`. `features/reports/components/report-list.tsx` (new), `app/(tenant)/t/[tenantSlug]/(dashboard)/reports/page.tsx` (previously a placeholder). `app/api/cron/outbox/route.ts` now links completed jobs to their `reports` row.
- `features/sales/components/product-grid.tsx` redesigned from a 2-column square-card grid to a vertical row list (thumbnail/name/description/price/quick-add button) — a visual-only change to the Capture Sales screen, no schema/service impact, adapted from a marketplace-app UI reference the user shared.
- `supabase/migrations/0014_insights_snapshots.sql` — applied and verified live (see 3d above). `InsightsService` (new) is now real: `evaluateDailyInsights`/`listRecent`. `features/analytics/components/insights-list.tsx` (new), wired into the Analytics page above the KPI tiles. `app/api/cron/outbox/route.ts` now also calls `evaluateDailyInsights` after a successful daily report.
- `supabase/migrations/0015_queue_daily_report_job_and_corrections_report.sql` — applied and verified live (see 3e above). `ReportService.generateCorrectionsReport` (new), `BusinessDayService.closeDay()` now queues a `report_jobs` row via `queue_daily_report_job()` on every close (manual or automatic — previously only automatic). `features/reports/components/report-list.tsx` now branches by `reportType`.
- `PermissionService.listCatalog`/`RoleService.{listRoles,getRole,createRole,updateRole,setRolePermissions,deleteRole}` (previously stubs/missing) are now real (see 4a above). New `validations/role.ts`, `features/roles/{actions,components}/`, `app/(tenant)/t/[tenantSlug]/(dashboard)/roles/page.tsx`. No new migration needed — the DB/RLS layer already existed from migration 0001.
- `UserService.{inviteUser,setActive,setUserRole,listUsers}` (previously stubs) are now real (see 4b above). New `validations/user.ts`, `features/users/{actions,components}/`, `app/(tenant)/t/[tenantSlug]/(dashboard)/users/page.tsx`. `lib/supabase/service-role.ts`'s allowed-callers list gained `UserService.inviteUser`. No new migration needed.
- `supabase/migrations/0016_login_events_and_sessions.sql` — applied and verified live (see 4c above). `SecurityService` (new), `lib/utils/user-agent.ts` (new), `features/security/{actions,components}/`, `app/(tenant)/t/[tenantSlug]/(dashboard)/security/page.tsx`. `features/auth/actions/sign-in.ts` now logs login events and creates sessions. `lib/tenant/resolve-active-tenant.ts`'s `ActiveTenant` gained `tenantId` (previously only `slug`/`needsOnboarding`) since the sign-in action needed it for `login_events.tenant_id`/`sessions.tenant_id`.
- `features/security/components/mfa-enrollment.tsx` — new (see 4d above). No new migration or service — calls `supabase.auth.mfa.*` directly on the browser client.

## Decisions made during setup (beyond the architecture-planning decisions logged in `19-security-checklist.md`)

- **Pinned Next.js to 15.5.23 instead of the `latest` tag (16.3.0).** `create-next-app@latest` pulled Next 16.3.0, whose own generated agent-notes explicitly warn of breaking changes from typical training data and instruct reading bundled docs before writing code. For a security-critical, long-lived foundation, stability and familiarity outweighed being on the newest release. Next 15 is still fully modern App Router/Server Actions/Server Components.
- **`npm audit` overrides for `postcss`/`sharp`.** Next 15.5.23 bundles vulnerable transitive versions of both; added `overrides` in `package.json` pinning safe versions without needing to move off Next 15. Audit is clean (0 vulnerabilities).
- **ESLint flat-config compatibility shim.** `eslint-config-next@15.5.23` ships legacy (non-flat) shareable configs; `create-next-app`'s generated `eslint.config.mjs` assumed the newer flat-config-native format. Fixed using the standard `@eslint/eslintrc` `FlatCompat` shim.
- **Hand-wrote `components/ui/form.tsx`.** The `shadcn` CLI's registry in this version doesn't ship a `form` block; hand-wrote the standard, well-established shadcn form-wrapper pattern (Controller/FormProvider context plumbing) rather than fighting the CLI.
- **`Relationships: []` gotcha in hand-written Supabase types.** Supabase's generic type system (`GenericTable`/`GenericSchema` in `@supabase/postgrest-js`) requires every table entry to declare a `Relationships` array for `.rpc()` argument type-checking to work at all — omitting it doesn't error loudly, it silently degrades every `.rpc()` call to an untyped fallback. Documented here so it isn't rediscovered the hard way later, and so nobody "simplifies" the provisional types file by removing it.
- **Missing `tenants.country` column.** The original scope's tenants table (source doc S103) specifies `country`; migration 0001 omitted it. Caught while building the sign-up form (which collects country per spec S9.1). Fixed via `0002_add_tenant_country.sql` rather than editing 0001 — applied and verified live.
- **`handle_new_auth_user()` needed to also capture `phone`.** It only read `full_name` from `raw_user_meta_data`. A separate post-signup `profiles` UPDATE would have silently no-op'd under RLS when email confirmation is required (no session exists yet at that point), so the fix is in the trigger itself (`0003_profile_phone_from_signup.sql`), keeping profile creation fully transactional with the `auth.users` insert. Applied and verified live (a test signup's profile correctly showed the captured phone).
- **Embedded PostgREST relationship selects (`tenant:tenants(slug)`) don't type-check against the provisional hand-written types**, since `Relationships` arrays are empty (no FK metadata modeled). Worked around by querying the two tables separately (`lib/tenant/resolve-active-tenant.ts`) rather than fighting the types now. Revisit once real generated types (with relationship metadata) are in place — see the `types/database.types.ts` note above.
- **shadcn's `base-nova` style uses Base UI, not Radix — `asChild` doesn't exist.** Composition uses a `render` prop instead (`<Button render={<Link href="...">...</Link>} />`). Different convention from "classic" shadcn/ui docs and examples found online; worth remembering before reaching for `asChild` anywhere else in this codebase.
- **A literal `*/` inside a `/** ... */` doc comment silently truncates the comment early**, turning the rest into a parse error (hit this writing a comment that said `features/*/actions`). Avoid literal `*/` sequences inside block comments — use `<name>` placeholders instead of `*` wildcards in comment prose.
- **Next.js App Router treats any folder prefixed with `_` as private (excluded from routing).** A scratch verification API route placed at `app/api/_scratch-e2e-test/` 404'd for exactly this reason — moving it out of the underscore-prefixed folder fixed it. Relevant if a private/non-routed co-location folder is ever intentionally wanted under `app/`.
- **Supabase's `auth.signUp()` (not `admin.createUser()`) validates email format more strictly** — it rejected `@test.local` addresses outright ("invalid" error) but accepted `@example.com` (RFC 2606 reserved test domain). `admin.createUser()` doesn't apply the same check.
- **No browser automation tool is available in this environment.** Phase 1c's forms were verified by proving the exact service-layer code they call (`AuthService`, `TenantService`, `RoleService`) works correctly against the live database from a temporary, git-ignored test route hit with `curl` — not by clicking through the rendered UI in a real browser. Phase 1e's verification went one step further (real login through the actual `/api/auth/callback`-adjacent auth flow, then a real GET of `/t/[slug]` with the resulting session cookie, confirming the server-rendered HTML), which is stronger evidence but still not a literal click-through — no test has yet driven the client-side RHF form submission via simulated user interaction. This is an honest gap, not a claimed pass.
- **Merged `development` into `main`** (fast-forward, no conflicts) after discovering the user's Vercel project's Production deployment tracks `main`, which had been sitting on just the initial foundation commit while all of Phase 1c/1e/branding landed on `development`. Going forward, push both together promptly rather than letting them drift.
- **Generated `favicon.ico` from the custom SVG mark via `sharp`**, hand-assembling a modern PNG-in-ICO container (16/32/48px) rather than a raw bitmap ICO, since `sharp`/libvips has no native ICO encoder. Documented here so the regeneration approach doesn't need rediscovering if the mark changes: rasterize `app/icon.svg` at each size with `sharp`, then concatenate an `ICONDIR` header + `ICONDIRENTRY` per image + the raw PNG bytes.
- **`app/layout.tsx`'s font CSS variable name didn't match what `app/globals.css` expected.** `create-next-app`/shadcn scaffolding set `--font-geist-sans`, but the `@theme inline` block maps `--font-sans: var(--font-sans)` — so the Geist font was silently never applied; the app had been rendering in the browser's default sans stack the whole time. Fixed as part of switching to Outfit (now correctly named `--font-sans`) — worth knowing this was already broken before the font swap, not a regression caused by it.
- **Zod `.transform()` breaks React Hook Form's `useForm<T>()` typing.** A schema with `.transform()` has a different input type (what the form holds pre-submit, e.g. `""`) than its output type (what `z.infer` gives you, e.g. `string | null` after the transform) — but `useForm<T>` and `zodResolver` both need one consistent type. Fixed by keeping `validations/onboarding.ts`'s schema transform-free (plain `string` output) and doing the `"" -> null` conversion at the point of use in the server action instead. Worth remembering before reaching for `.transform()` in any other RHF-backed schema.
- **No new migration needed for the onboarding wizard's real steps** — `tenants` already had `business_type`/`website`/`anniversary_date`/`currency`/`timezone` from migration 0001, and `locations`/`location_hours` already existed too. Deliberately scoped Phase 1d's real forms to what the existing schema already supported, rather than expanding scope to match every spec field (e.g. logo upload, geofence coordinates) in this pass.
- **`resolveActiveTenant()`'s `needsOnboarding` flag is inferred from `locations` count being zero, not a dedicated `onboarding_completed_at` column.** Chosen deliberately to avoid a migration for a flag with only one real consumer right now; correct for Phase 1's one-location-per-tenant reality since the wizard's Step 2 is what creates the first location. Revisit if a tenant should ever be able to legitimately have zero locations while still being "onboarded" (unlikely before multi-location features land).
- **Zod string-refine instead of `.coerce`/`.transform()` for a numeric RHF field — same root cause as the earlier onboarding gotcha, new shape.** `validations/product.ts`'s `expectedPrice` needed numeric validation, but `z.coerce.number()` broke `useForm<CreateProductInput>()` + `zodResolver` (coerce's input type is `unknown`/`string`, its output type is `number` — RHF needs both identical). Fixed by keeping `expectedPrice` a validated `string` (`.refine()` checking it parses to a number `>= 0`), with the string→number conversion happening only at the point of use in `create-product.ts`'s server action (`Number(parsed.data.expectedPrice)`). Confirms the rule from 1d's decision log generalizes to `.coerce` as well as `.transform()` — any Zod method that changes the input/output type breaks RHF's `useForm<T>()`, not just `.transform()` specifically.
- **`sonner`'s `<Toaster />` was installed but never mounted anywhere.** The record-sale success toast (`features/sales/components/product-grid.tsx`) silently did nothing until this was caught by grepping for `Toaster` across `app/` and finding zero matches. Fixed by adding it to `app/layout.tsx`. Worth checking for on any future toast-library install — the import succeeding doesn't mean the portal is rendered.
- **Supabase Storage's `remove()`/`list()` need a SELECT policy on `storage.objects`, not just DELETE — a public bucket does NOT substitute for it.** Migration 0007 shipped with insert/update/delete policies only (reasoning: the bucket is public, so reads don't need RLS). Live verification caught the real consequence: replacing a product's image reported success (`ProductService.setImage` returned no error) but the OLD file was still sitting in Storage afterward. Root cause: Storage's authenticated SDK operations (`remove`, `list`, `download`) perform an internal RLS-gated lookup against `storage.objects` before acting — a public bucket only bypasses RLS for the separate public-URL read path (`/storage/v1/object/public/...`), never for the authenticated API path. With no SELECT policy, that internal lookup silently matched zero rows, so `remove()` "succeeded" against nothing. Fixed in `0008_product_images_storage_select_policy.sql` (added after 0007 was already applied live, per the "never edit an applied migration" rule) with a SELECT policy gated on `is_tenant_member` rather than `products.edit`, since viewing/listing shouldn't require edit rights. **Generalize this**: any future private-write/public-or-shared-read Storage bucket needs an explicit SELECT policy for the SDK's mutating operations to work correctly, even if reads themselves don't need one.
- **Sale mutation is done through `SECURITY DEFINER` SQL functions, never an RLS UPDATE policy on `sales`.** Every candidate design that used an UPDATE policy needed either column-level grants (only `status` writable, not `actual_amount`/etc.) or a check constraint contortion to stop a client from directly rewriting a sale's amount. Two owner-run functions (`void_sale`/`correct_sale`) that internally call `has_permission()` and read `tenant_settings` sidestep that entirely — no policy grants UPDATE on `sales` at all, so there is no direct-write path to bypass, matching the exact pattern `assign_sale_number()` and `sale_number_sequences` already established in migration 0005. Two more owner-only internal helpers (`_apply_sale_void`/`_apply_sale_correction`, never granted to `authenticated`) hold the actual mutation logic so the "apply immediately" and "apply after approval" code paths in `void_sale`/`correct_sale`/`resolve_approval_request` share one implementation instead of two that could drift apart.
- **Approval requests are always created, even when auto-approved.** Considered skipping the `approval_requests` row entirely when a tenant doesn't require review (fewer rows, simpler code), but that would mean the audit trail's shape depends on a tenant setting — a `sale_corrections` row with no matching `approval_requests` row in one tenant, and one with it in another. Writing the row unconditionally (status `auto_approved` vs `pending`) keeps the audit trail and the dispatch logic identical in both cases, at the cost of one extra always-empty-ish table scan. Matches the architecture decision made during initial planning (see the plan-mode summary this session started from).
- **`tenant_settings` (existing generic key/value jsonb table from migration 0001) holds the new approval/edit-window config** (`sale_void_requires_approval`, `sale_correction_requires_approval`, `sale_edit_window_minutes`) instead of new dedicated columns — no migration needed for tenant-level configurability, and a missing row cleanly defaults (`false`/`false`/`15`) via `COALESCE`. Consistent with how the table was already designed to be used.
- **Live-verified 1g and Phase 2 (2a–2d) via a single temporary scratch API route** (`app/api/scratch-verify/route.ts`, deleted immediately after the run, never committed — confirmed via `git status` showing a clean tree). It exercised the real `PlatformAdminService`/`TenantService`/`ProductService`/`BusinessDayService`/`SalesService` classes end-to-end: service-role for setup (creating two test auth users, granting one `platform_admins`, bootstrapping a test tenant via `TenantService.createTenant`), then an actual `signInWithPassword()` session (anon key + `Authorization: Bearer <access_token>` header) for every RLS-gated call after that, so the assertions prove real RLS policy behavior, not service-role-bypassed behavior. All 15 assertions passed on the first run against the live database. Cleanup (delete `platform_admins` row, delete tenant — cascades products/business_days/sales/locations/roles/memberships/sale_number_sequences, delete both auth users) ran in a `finally` block and was confirmed via dev-server log inspection. Note: a scratch route folder must not be named with a leading underscore (`_scratch/...`) — Next.js treats that as a private, unrouted folder (see the existing 404 gotcha above); this attempt hit that same wall first and was moved to `app/api/scratch-verify/` before it worked.

## Blockers

None currently blocking. Follow-ups, none gating further work:

1. **pgTAP suite not yet executed.** Needs Docker Desktop running locally (`supabase start` + `supabase test db`). The schema itself has been spot-verified against the live project (see 1b/1c above), so this is about getting the full 20-assertion proof running, not about unverified risk.
2. **`types/database.types.ts` regeneration needs a Supabase personal access token.** Not blocking — the hand-written version is confirmed accurate against the live schema, including the 0002/0003 additions.
3. **Real browser verification of the auth forms hasn't happened** (no browser automation tool available). The service-layer logic behind every form is proven; the React/RHF wiring in front of it is not yet visually confirmed. Worth a manual click-through in an actual browser before Phase 1c is called fully done, or once a browser tool becomes available.
4. **Supabase's default email provider has a low sending rate limit**, hit during verification. Sign-up/password-reset emails will need a custom SMTP provider (Resend, per the original stack — see `14-billing-paystack.md`'s sibling doc `13-notifications.md`) configured in the Supabase dashboard before real users can reliably receive confirmation emails. Not a code change; a dashboard configuration step for later.

## Next recommended action

**Phase 1 is now fully done** (1a–1g). **Phase 2 is now fully done** (2a–2i all verified live, plus the 2f-bug sale-number fix applied and verified) except REVERSE and 2h's MFA-gating, both deliberately deferred (see below). **Phase 3 is now fully done** (3a–3e: KPI dashboard, product analytics, daily report generation, rule-based insights, corrections/void reports), all live-verified, except weekly/monthly/custom report types, Resend delivery, and Smart alerts/Daily Pulse, all deliberately deferred. **Phase 4 — Enterprise Controls is now fully done** (4a–4h: custom roles UI, users & invitations, security centre, MFA enrollment, working-hours login restriction, geo-fencing + temporary-access-request, download security, full `audit_logs` coverage pass), all live-verified. **Phase 5 — Data Migration is underway**: 5a (historical sales import) done, live-verified.

Immediate next step (per the roadmap, in order):
- Continue **Phase 5 — Data Migration**: products bulk upload (`type = 'products'`, reuses 5a's `imports`/`import_rows` schema and UI shell) is the one remaining Phase 5 item — then Phase 6 (Billing/Paystack). 2h's MFA-gated reopen (deferred from Phase 2) can still be revisited whenever convenient — enrollment exists (4d), so it's an explicit decision away from wiring, not blocked on missing infrastructure. Admin-driven forced termination of a specific other live session remains an explicit gap from 4c, worth a decision before promising it in the UI as more than tracking/audit.

Remaining Phase 2 work (deliberately deferred, not blocking Phase 3):
- **REVERSE** (deferred from 2e — VOID and CORRECT cover the two real cases this phase targeted; REVERSE is the third documented mutation type, not yet needed by anything built so far).
- **2h's MFA-gating** (deferred to Phase 4 per explicit decision — the approval-routing half already exists via 2g).
- Product bulk upload (spec S45, `docs/10-products.md`'s "Bulk upload" section) and drag-and-drop `display_order` reordering (`ProductService.reorder()` is still a stub) — both explicitly out of scope for the product-enhancements batch completed earlier.

Also still pending, not gating anything:
- next-intl scaffold, deferred from 1f — pick up whenever i18n actually becomes relevant.
- Start Docker Desktop and run `supabase start` + `supabase test db` to get the pgTAP suite (1b) actually executing rather than just written.
- Confirm the Vercel production deployment (env vars + `NEXT_PUBLIC_APP_URL` + Supabase redirect URL) actually builds and serves correctly now that `main` is current.
- Sale-number template becoming tenant-configurable (deferred from 2c — currently hardcoded `SALE-{YYYY}-{000001}`).
